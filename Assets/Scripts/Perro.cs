using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 *este script lo que hace es tener un método que se llame GetDireccion() y que haga overwrite del metodo de agente
 * hereda de comportamiento agente y el namespace. Dentro del script flocking se juntan las direcciones de seguir separacion evasioon colisiones y evasion obstaculos.
 
     
*/
public class Perro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
